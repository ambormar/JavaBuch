/* TODO 12.4.2.  eigene bemerkung 
 * AtOverride
 * 
 * 	@Override 	= wie ein kommentar bei absichtlichem überschreiben von methoden der superklasse. (um zu klären, dass methoden nicht aus versehen überschrieben wurden)
 * 				-> kompiler nimmts zur kenntnis, s hat aber noch keine konsequenzen in java 7
 * 				-> heute java7: ist's guter programmierstil
 * 				-> evtl. ab java 8 oder 9 unumgänglich, 
 * 
 * 		BSP:	@Override																					
 *				protected void paintComponent(Graphics g){	
 *					if (image != null){
 *						g.drawImage(image, 0, 0, this);
 *					}
 *				}
 *				
 */

package uebungen12;

public class AtOverride {

}
